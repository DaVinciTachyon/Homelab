---
app:
  networkPolicy:
    enabled: true
  spec:
    egress:
      - ports:
          - port: 80
ingress:
  enabled: true
  annotations:
    cert-manager.io/cluster-issuer: "selfsigned-issuer"
  hosts:
    - kube.home
  tls:
  - hosts:
    - kube.home
    secretName: kubernetes-dashboard-cert